name: release server
on: 
  push:
    tags:
    - 'mfl-*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo
      uses: actions/checkout@master
    - name: get version from GITHUB_REF
      id: get_version
      run: |
        GITREV="${GITHUB_REF/refs\/tags\//}"
        echo "VERSION=${GITREV:4}" >> "$GITHUB_OUTPUT"
    - name: write version to versionfile
      run: echo "${{steps.get_version.outputs.VERSION }}" > "./server/app/version"
    - name: create zip
      run: |
        zip -r "./mfl-${{steps.get_version.outputs.VERSION }}-server.zip" ./api/* ./config/* ./db/* ./mailtemplates/* ./resources/* ./utils/* ./main.py ./log-config.ini ./requirements.txt ./server-config-default.ini ./version
    - name: Upload zip to release
      uses: softprops/action-gh-release@v2
      with: 
        files: "./server/mfl-${{steps.get_version.outputs.VERSION }}-server.zip"
